const ROOT_ROUTE = `/role-permission`;

const SEARCH_TYPES = [{ value: 'permission', isDefault: true }, { value: 'role' }];

const PERMISSION_KEYS = {
  VIEW_ROLE_AND_PERMISSION: 'VIEW_ROLE_AND_PERMISSION',
};

export { ROOT_ROUTE, SEARCH_TYPES, PERMISSION_KEYS };
