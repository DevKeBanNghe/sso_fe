const ROOT_ROUTE = `/user-role`;

const SEARCH_TYPES = [{ value: 'user', isDefault: true }, { value: 'role' }];

const PERMISSION_KEYS = {
  VIEW_USER_AND_ROLE: 'VIEW_USER_AND_ROLE',
};

export { ROOT_ROUTE, SEARCH_TYPES, PERMISSION_KEYS };
